@model SiteText
@{
    ViewData["Title"] = Model.Legal.TermsTitle;
}

<section class="legal">
    <div class="container">
        <a class="legal-back" href="@Url.Action("Index", "Home")">@Model.Legal.BackHome</a>
        <h1>@Model.Legal.TermsTitle</h1>
        <div class="legal-content">
            @Html.Raw(Model.Legal.TermsHtml)
        </div>
    </div>
</section>
